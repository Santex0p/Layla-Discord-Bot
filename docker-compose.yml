services:
  bot-layla:
    build: .
    image: layla-bot:1.0
    container_name: layla-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    networks:
      - cloudflared-net

networks:
  cloudflared-net:
    external: true
